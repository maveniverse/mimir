# Mirror Fail

This test makes Mimir use mirror support that it goes over all of them and falls back to main repo.

local-release-mirror is asked first (by conf) but the wanted artifact is present in local-release-main ONLY.